<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Conexão com o banco de dados MySQL -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/loja?createDatabaseIfNotExist=true</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">1234</property>

        <!-- Dialeto SQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Exibe e formata o SQL gerado no console -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Cria o esquema de banco de dados na inicialização -->
        <!-- Use 'create' para gerar o DDL e tabelas toda vez que rodar, que substitui o banco -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapeamento das Entidades -->
        <mapping class="com.exemplo.modelo.Produto" />
        <mapping class="com.exemplo.modelo.ProdutoEletronico" />
        <mapping class="com.exemplo.modelo.ProdutoPerecivel" />
        <mapping class="com.exemplo.modelo.Item" />
        <mapping class="com.exemplo.modelo.Pedido" />
    </session-factory>
</hibernate-configuration>
